Work in the current React Native multi-TV monorepo.
Implement the completed workshop application directly from these requirements:

  1. Shared Header
  - Create packages/shared/src/components/Header/Header.tsx.
  - Display “Hello {platform},” using Platform.select:
    - iOS, Android, Web, default Vega.
  - Add the subtitle: “Select one of the options to start your development
  journey 🚀”.
  - Add scaled TV styling.
  - Create HeaderLogo.tsx and HeaderLogo.kepler.tsx using assets/vega.png.
  - Create HeaderLogo.web.tsx using assets/web.png with a white tint.

  2. Animation
  - Create IconReactNativeAnimated/IconReactNativeAnimated.tsx.
  - Use lottie-react-native, play Animation-rn-logo.json on mount, and size it
  with the scaling utilities.
  - Create a .web.tsx variant that continuously rotates react-logo.png using
  React Native Animated.
  - Stop the web animation when unmounted.
  - Preserve or verify the Vega Metro alias from lottie-react-native to
  @amazon-devices/lottie-react-native.

  3. API demo
  - Create services/httpClient.ts containing a reusable fetch client.
  - Support base URL, default headers, query parameters, JSON request bodies,
  GET and POST.
  - Implement request timeouts using AbortController.
  - Create components/ApiDemo.tsx.
  - Fetch a random joke from:
    https://official-joke-api.appspot.com/random_joke
  - Show loading, error, setup, and punchline states.
  - Use a 10-second timeout.

  4. Tiles and HomeScreen
  - The tiles must appear in this order:
    - home
    - animation (“Animated\nDemo”)
    - movies (“Movies”)
    - api-demo (“API\nDemo”)
  - Use the existing icons: home.png, get-started.png, debug.png, and learn-
  more.png.
  - Update HomeScreen so:
    - Home displays Header.
    - Animation displays IconReactNativeAnimated.
    - Movies displays MovieList.
    - API Demo displays ApiDemo.
  - Keep TV focus handling, preferred Home focus, accessibility labels,
  scaling, and existing background.
  - Keep the last-focused content mounted when focus moves into the movie list.

  5. Movie list
  - Create data/catalog.ts with Movie and Catalog types, fetchCatalog, and a
  useMovies hook.
  - Fetch the catalog from:
    https://giolaq.github.io/scrap-tv-feed/catalog.json
  - Create a shared MoviePoster with focus scaling.
  - Create MovieList/MovieList.tsx using a horizontal FlatList for Expo TV and
  web.
  - Create MovieList/MovieList.kepler.tsx using
  @amazon-devices/vega-carousel and TVFocusGuideView.
  - Guard out-of-bounds Carousel item requests and do not mount it with an
  empty adapter.
  - Add @amazon-devices/vega-carousel only to the Vega workspace.

  6. Shared exports
  - Export Header, HomeScreen, ApiDemo, MovieList, MoviePoster, fetchCatalog,
  useMovies, createHttpClient, and their public types from
  packages/shared/index.ts.

  7. TypeScript and tests
  - Ensure the shared TypeScript configuration can resolve lottie-react-
  native.
  - Replace stale Vega template snapshot tests with meaningful workshop tests.
  - Verify the Header, workshop tiles, focused content, and movie-list states.
  - Remove obsolete snapshot files.
  - Configure Vega Jest to resolve React, React JSX runtime, Babel runtime,
  and sibling shared-workspace imports.
  - Add lightweight Lottie, Carousel, and media Jest mocks where required.
  - Add an after-environment requestAnimationFrame shim using Date.now to work
  around the Vega Jest preset’s unsupported jest.now call.
  - Fix the shared Jest preset so its existing scaling tests run.
  - Do not weaken production code or disable meaningful checks.

  8. Validation
  - Preserve unrelated user changes.
  - Do not modify yarn.lock unless dependencies genuinely change.
  - Install dependencies using the Yarn version pinned by the repository.
  - Run:
    - Shared TypeScript check
    - Shared tests
    - Vega tests with Watchman disabled
    - Vega lint
    - git diff --check
    - Vega debug build
  - Confirm these artifacts exist:
    - packages/vega/build/aarch64-debug/vega_aarch64.vpkg
    - packages/vega/build/armv7-debug/vega_armv7.vpkg
    - packages/vega/build/x86_64-debug/vega_x86_64.vpkg

Report the files changed, checks passed, build artifacts, and any remaining warnings.
